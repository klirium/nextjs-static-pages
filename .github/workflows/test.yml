name: GitHub Actions Demo
run-name: ${{ github.event.pull_request.title }} is testing out GitHub Actions 🚀
on:
  pull_request:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest
    if: startsWith(github.head_ref, 'data-')
    steps:
      - name: show1
        env:
          value1: "${{ env.value1}} hi there"
        run: |
          echo "Value3"
