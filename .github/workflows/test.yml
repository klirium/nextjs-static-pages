on:
  workflow_dispatch:
    inputs:
      value1:
        description: "Dataflow to run"
        required: true
        default: "test2"
        type: string
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Run step
        run: |
          echo "Running echo step: ${{  github.event.inputs.value1 }}"
