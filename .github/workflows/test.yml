on: push
env:
  DAY_OF_WEEK: Monday

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: Hey
        run: echo "action $GITHUB_ACTION"
      - name: HeyA
        run: echo "action $GITHUB_ACTION_PATH"
