on: [push]

jobs:
  job_1:
    name: Add 3 and 7
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          echo ${{github.workspace}} && echo ${{github.event_name}} && echo ${{github.sha}}
          echo ${{github.workspace}} && echo ${{github.event_name}} && echo ${{github.sha}}
