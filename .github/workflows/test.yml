on: push
env:
  DAY_OF_WEEK: Monday

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: Hey
        id: hey-id
        run: echo "action $GITHUB_ACTION"
      - name: HeyA
        id: hey-idA
        run: echo "action $GITHUB_ACTION"
