name: GitHub Actions Demo
run-name: ${{ github.ref }} is testing out GitHub Actions 🚀
on:
  push:

jobs:
  build:
    runs-on: ubuntu-runners
    steps:
      - name: show1
        run: |
          echo "Value3"
