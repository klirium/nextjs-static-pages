name: GitHub Actions Demo
run-name: ${{ github.ref }} is testing out GitHub Actions 🚀
on:
  pull_request:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: show1
        env:
          value1: "${{ env.value1}} hi there"
        run: |
          echo "Value3"
