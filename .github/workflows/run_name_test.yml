name: Run name test
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: show1
        env:
          secret1: ${{ secrets.GITHUB_TOKEN }}
        run: echo "${{ secrets.GITHUB_TOKEN }}" && echo "${{ env.secret1 }}" && echo $secret1 && echo "$secret1"
