name: GitHub Actions Demo
on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  print-tag:
    runs-on: ubuntu-latest
    steps:
      - name: Print the input tag to STDOUT
        run: echo  The tags

